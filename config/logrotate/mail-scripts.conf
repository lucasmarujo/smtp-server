# Logrotate (HOST) para os logs de cron do backup/healthcheck.
# Copiar para /etc/logrotate.d/mail-scripts

/opt/mail/backups/backup.log /opt/mail/backups/healthcheck.log {
	weekly
	rotate 4
	missingok
	notifempty
	compress
	delaycompress
	copytruncate
	su lucas lucas
}

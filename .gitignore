# Secrets / ambiente
.env

# Dados persistentes (mailbox, estado, logs, chaves DKIM, contas)
docker-data/

# Backups
backups/*.tar.gz
backups/*.tar.gz.sha256
backups/*.tar.gz.gpg
backups/*.tar.gz.gpg.sha256
backups/*.log

# Credenciais geradas
secrets/
*.private
credentials-*.txt

backups/caddy/*.bak